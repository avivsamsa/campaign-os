# Campaign OS — אפליקציית פורטל לקוח (React Native / Expo)

אפליקציה נייטיב לפורטל הלקוח. מדברת עם ה-backend הקיים (`ppc.avivsamsa.co.il`) עם אימות **Bearer token**.

## הרצה (פיתוח)
```bash
cd mobile
npm install
npx expo install --fix      # מיישר גרסאות ל-SDK הנכון
npx expo start
```
- סרוק/י את ה-QR עם **Expo Go** (מה-App Store) בטלפון, או `i` לסימולטור iOS / `a` לאנדרואיד.
- התחבר/י עם חשבון הדמו: פורטל `demo`, סיסמה `demo2026` (או כל לקוח אמיתי).

> RTL: בהרצה הראשונה ייתכן שיידרש reload אחד (r ב-terminal) כדי שהעברית תתיישר לימין.

## מבנה
- `app/` — מסכים (Expo Router): `login`, `leads`, `lead/[id]`
- `lib/` — `api.ts` (קריאות ל-backend), `auth.tsx` (token ב-SecureStore), `theme.ts`, `config.ts`
- `config.ts` → `API_BASE` (כתובת ה-backend)

## מה יש (v1 מוגמר)
- **התחברות** (slug + סיסמה) → token נשמר מאובטח (SecureStore)
- **רשימת לידים** — אנליטיקות, חיפוש (שם/טלפון), פילטר סטטוס, שער קטגוריות (ללקוח רב-קטגוריות), pull-to-refresh + רענון בכניסה למסך, חיוג/וואטסאפ
- **מסך ליד** — חיוג/וואטסאפ, שינוי סטטוס מלא (רכישה→סכום · לא רלוונטי→סיבה+"אחר" · סטטוסים מותאמים), יומן/הערות עם צבע לפי סוג
- **חשבון** — התנתקות, מחיקת חשבון (Apple 5.1.1v) עם הגנת דמו

## צ'קליסט בדיקות (התחבר עם demo / demo2026)
- [ ] התחברות עובדת · סיסמה שגויה מציגה שגיאה
- [ ] רשימת לידים נטענת · אנליטיקות נכונות
- [ ] חיפוש מסנן · צ'יפי סטטוס מסננים
- [ ] חיוג ווואטסאפ נפתחים
- [ ] מסך ליד: "רכישה" → פופאפ סכום · "לא רלוונטי" → סיבה/"אחר" · סטטוס רגיל
- [ ] הוספת הערה עובדת · חוזרים לרשימה והיא מתעדכנת
- [ ] חשבון → התנתקות · מחיקת חשבון (בדמו: "מושבת")
- [ ] RTL יושב לימין (אם לא — reload אחד: `r` ב-terminal)

## עדיין לא (דורש הגדרה/דיון יחד)
- **פוש** (ליד חדש / בעיית חשבון) — דורש Firebase + חבילות נייטיב
- **Face ID** — דורש `expo-local-authentication`
- הגשה ל-App Store (EAS)

## בנייה לחנות (EAS)
```bash
npm i -g eas-cli && eas login
eas build:configure
eas build --platform ios        # בונה .ipa בענן (בלי Xcode מקומי)
eas submit --platform ios       # מעלה ל-App Store Connect
```
